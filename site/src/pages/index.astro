---
import Base from '../layouts/Base.astro';
import { site } from '../lib/site';
---

<Base>
  <h1>{site.name}</h1>
  <p>{site.tagline}.</p>
  <p>Placeholder page — the site is being built. No prices are shown yet.</p>
</Base>
