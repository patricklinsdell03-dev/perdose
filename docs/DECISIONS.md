# Decisions log

Add a row whenever a rule, factor, model id, schema, or scope changes. Cite sources for factor changes.

| Date | Decision | Why | Alternatives considered | Affects |
|---|---|---|---|---|
| 2026-09-17 | Adopt brief v1.4 | — | — | all |
| 2026-09-17 | Model ids confirmed: default `claude-haiku-4-5-20251001`, escalation `claude-sonnet-5`. Checked against the models overview at https://docs.claude.com (redirects to https://platform.claude.com/docs/en/about-claude/models/overview). Both are listed as current Claude API ids. **Watch:** the same page lists Haiku 4.5 retirement as "not sooner than 2026-10-15" — re-check before Phase 2 and choose a successor if one is announced. The structured-output mechanism is not checked yet; that check belongs to Phase 2 (brief §9.2). | CLAUDE.md requires the check before first use | — | `config/llm.yml` |
| 2026-09-17 | `daily.yml` ships with manual trigger only; the cron line is present but commented out until Phase 5. | Until Phase 3 `make all` cannot succeed, so a live cron would fail (and email) every day. Brief §17 puts the working cron in Phase 5. | Enable now and let it fail; make unbuilt commands exit 0 (rejected: a silent no-op looks like a successful run) | `.github/workflows/daily.yml` |
| 2026-09-17 | Pipeline subcommands that are not built yet print "not built yet — Phase N" and exit 1. Only `golden` runs (on an empty set). | Honest failure beats silent success | Exit 0 stubs | `pipeline/cli.py`, `make all` |
| 2026-09-17 | **Astro 7.x instead of the brief's 5.x** (installed 7.3.3; TypeScript held at 6.x because `@astrojs/check` does not support 7 yet). Patrick approved. | `npm audit` flagged 5.x with 3 advisories incl. one critical; 7.3.3 audits clean. Cheapest to move while the site is one page. | Stay on 5.x per brief §4 | `site/package.json`, brief §4 |
| 2026-09-17 | **Patrick chooses the LLM models himself; the brief's Haiku/Sonnet recommendation (§9.2, CLAUDE.md stack) no longer binds.** `config/llm.yml` keeps the brief's ids as placeholders only; Patrick sets the real ones before Phase 2. Do not pick or change models without asking him. | Patrick's instruction | — | `config/llm.yml`, brief §9.2, §20.3 |
| 2026-09-17 | Unit conversion is generic: every amount is converted to the compound's own declared unit (mg / mcg / IU, using that compound's `unit_conversions`), rather than the brief's literal "IU only valid for D3". Patrick approved. | Needed for B12 "1 mg" → 1000 mcg (golden #30) and Appendix F vitamins A/E | Special-case per compound | brief §9.5 step 1, Phase 1 rules |
| 2026-09-17 | Golden-label conflicts and factor sources: Patrick delegated to Claude's judgement. Each resolution is logged with reasons when made in Phase 1. | — | — | Phase 1 |
| 2026-09-17 | **Elemental factors verified by calculation.** Each factor is recomputed from the substance's chemical formula and IUPAC standard atomic weights; formulas live in `config/factor_sources.yml` and `tests/test_factors.py` fails if `compounds.yml` drifts > 1.5 %. All Appendix A factors confirmed except two changed: **zinc citrate 0.31 → 0.321** (dihydrate, the commercial form; anhydrous would be 0.341 — 0.31 matches neither) and **magnesium malate 0.15 → 0.155** (anhydrous 1:1, matching the brief's "anhydrous unless noted" convention). | NIH ODS pages refuse automated reads (HTTP 403); a formula calculation is reproducible and testable. | Cite fact sheets by hand | `config/compounds.yml`, `config/factor_sources.yml` |
| 2026-09-17 | **Open risk, magnesium malate:** products sold as "magnesium malate" range from ~11.6 % Mg (trihydrate) to ~19 % (dimagnesium malate), and the brief puts both under one form. Estimates for malate are therefore rough (shown as "estimated"; stated elemental always wins). Revisit with real labels in Phase 3 — options are splitting the form or removing the factor so compound-only malate labels go to review. | Honest uncertainty (brief §1) | — | `config/compounds.yml` |
| 2026-09-17 | **Golden #5 changed** (Magnesium Oxide 500mg 90 tabs): Appendix B expected "≈ 301.5 mg estimated"; now expects `needs_review: ambiguous_basis`. | Oxide is not in the §9.5 "≥ 400 mg means compound" list, and rightly: 500 mg elemental from ~830 mg oxide fits one tablet, so an unqualified "500mg" could be either. Ambiguous = not ranked (hard rule 3); a wrong guess here is a 40 % price error. When a label says it is the oxide mass, the factor still applies (tested). | Add oxide to the heuristic list | `tests/golden/labels.yml`, Appendix B |
| 2026-09-17 | **Golden #9** ("stated (convention)"): recorded as `amount_basis = stated_elemental` with confidence capped at 0.8. The same rule is used for every `elemental_default` compound when the label does not qualify the number. | `stated (convention)` is not an allowed `amount_basis` value in §5.2 | New basis value (schema change) | rules, golden set |
| 2026-09-17 | **Golden set is 37 labels, not 30:** Appendix B's 30 plus 7 synthetic extras (g31–g37), because §15 requires ≥ 3 labels per compound and Appendix B gives ashwagandha 2, vitamin C 1, B12 1, L-theanine 0. Threshold stays 90 % (34/37 live); calc-only mode must be 100 %. Each label carries a hand-written reference extraction so rules + maths are testable without an LLM. | §15 "rule for growth" vs Appendix B count | Relax the ≥ 3 rule for the prototype | `tests/golden/labels.yml` |
| 2026-09-17 | **Label rules and the `Extraction` schema were built in Phase 1**, not Phase 2 as §17 lists them. | Phase 1's own acceptance test needs `tests/test_rules.py` green and `make golden` in calc-only mode, which need both. Phase 2 keeps: LLM client, cache, prompt, escalation. | — | brief §17 |
| 2026-09-17 | **Rule details the brief leaves open** (all in `pipeline/normalise/rules.py`): (a) an evidence quote must actually occur in the title/description or the number is discarded; (b) magnesium heuristics are YAML parameters (`heuristics.unclear_amount`), not code; (c) product-level `needs_review` follows the **primary** active — a secondary active with a problem is just not rank-eligible itself; (d) an ambiguous amount is stored as null so it can never be ranked; (e) the model's free-text `review_reasons` are kept as notes but do not force review — only code-defined reasons and the 0.6 confidence floor do; (f) missing units-per-serving on capsules/tablets assumes 1 with −0.1 confidence; on powders/liquids it means servings unknown → review; (g) omega-3 with only one of EPA/DHA stated sums what is stated (DHA-only algae oils are legitimate). | Needed to implement §9.5; each is the conservative reading | — | rules |
| 2026-09-17 | **Open for Phase 3:** the negative fixtures "magnesium bath flakes" and "multivitamin" match compound aliases, so the §7.4 alias filter alone will not drop them; ingest will need an exclusion list. Stored under `negatives:` in the golden file, not yet tested. | — | — | ingest |
| 2026-09-17 | **Model choice (Patrick): Claude Sonnet 5 (`claude-sonnet-5`) for all extraction.** Id verified on docs.claude.com the same day. Consequences: (a) **no temperature setting** — Sonnet 5 rejects sampling parameters with an error, so the brief's "temperature 0" is dropped; (b) **"escalation" is now a second attempt on the same model at higher thinking effort** (`effort: low` → `effort: high`), since there is no bigger model in play. The three escalation triggers from §9.2 are unchanged. Patrick can set different ids/efforts in `config/llm.yml` at any time. | Patrick's instruction; API behaviour per current docs | Haiku default + Sonnet escalation (brief) | `config/llm.yml`, brief §9.2 |
| 2026-09-17 | **Structured output mechanism:** the SDK's `client.messages.parse(output_format=<pydantic model>)` (structured outputs; the API constrains the reply to our JSON schema). The API-facing model (`LlmExtraction`) has every field required and evidence as a list of `{field, quote}` pairs, because structured outputs cannot express free-form `{field: quote}` objects; it is converted to the brief's `Extraction` immediately. Checked against the current SDK docs. | Brief §9.2 asks for the check to be recorded | Tool-use with a strict schema; plain JSON prompting | `pipeline/normalise/llm.py` |
| 2026-09-17 | **Prompt v1.0.0 differs from Appendix C in four places:** units-per-serving is left null when unstated (the code assumes 1 and lowers confidence, rather than the model doing it); "do not convert units" made explicit (code converts); fixed component names (`compound_mass`, `extract_mass`, `herb_equivalent`, `total_omega_3`, `EPA`, `DHA`) so the rules can find them; evidence format as above. "Output JSON only" removed — the API enforces it. | Keeps "model extracts, code computes" strict | — | `pipeline/normalise/prompt.py` |
| 2026-09-17 | **Golden modes:** `make golden` always runs calc-only (must be 100 %) and then replays saved LLM extractions from `tests/golden/cache/` if any exist for the current prompt version (must be ≥ 90 %). `make golden-live` (new target; also `PERDOSE_LIVE_LLM=1`) calls the real model and saves the results. | Brief §15 replay/live split; an extra make target is easier than an env var on Windows | — | `Makefile`, `pipeline/golden.py` |
| 2026-09-17 | **Live golden results (Sonnet 5).** Prompt v1.0.0: 31/37 (84 %), escalation 41 % — fail. Cause: the model lowered its own `confidence` because the *label* was ambiguous (already captured by `amount_refers_to: unclear`), tripping the 0.6 floor and the 0.7 escalation trigger; and it sometimes omitted pack-size evidence quotes. **Prompt v1.1.0** redefines confidence as "did I read the listing correctly", spells out both evidence lists, and tells the model to use a compound's `unspecified` form when the label names none. Result: **36/37 (97 %), escalation 14 %** — inside the §15 thresholds (≥ 90 %, < 15 %). The one miss (g21) was a code issue, fixed without a prompt change: component evidence keyed `components.EPA` is now accepted as well as `EPA`; replay is then 37/37. The model had also summed EPA+DHA into an unevidenced "1100" — the no-quote rule correctly discarded it and the code did the sum. | Phase 2 acceptance | — | prompt, rules, `tests/golden/cache/` |
| 2026-09-17 | **`unspecified` forms added** (no ids changed): vitamin C gets form `unspecified` (class `vc_standard`) for labels that say only "Vitamin C"; magnesium `other` and zinc `other` gain the name `unspecified`. Creatine deliberately has none — "Creatine 3000mg" with no form goes to review rather than into `cr_other` beside buffered/nitrate products. | Plain "Vitamin C 500mg" labels are common and would otherwise all be unranked | Map plain vitamin C to `ascorbic_acid` (a guess) | `config/compounds.yml` |
| 2026-09-17 | **Seed data collected by Claude, not supplied by Patrick** (changes hard rule 1's "CSVs that Patrick supplies"; Patrick's instruction). 71 real listings read from Bulk, Myprotein and Holland & Barrett product pages, one page at a time in a browser, the same thing a person would do by hand. Every row carries its source URL and date; every inference is listed in `data/seed/README.md`. Still true: nothing invented, no bulk crawling, and live affiliate feeds replace this in Phase 6 (brief §16 data provenance). Only factual label sections are stored — no marketing copy or reviews. | Patrick delegated it | Patrick builds the CSVs by hand (brief §7.1) | `data/seed/` |
| 2026-09-17 | **Seed price = the promoted selling price on the day**, not the struck-through "was" price. | It is what a buyer pays and what a feed's `search_price` carries; all three retailers run near-permanent promotions. Effective-price handling stays v1.1. | — | seed CSVs |
| 2026-09-17 | **Ingest exclusion list** (`config/ingest.yml`): listings whose title contains e.g. "bath flakes", "multivitamin", "whey" are dropped even if they mention a compound alias. | Appendix B's negatives (magnesium bath flakes, multivitamin) match compound aliases, so §7.4's alias rule alone keeps them. | Drop them later via extraction (costs an LLM call each) | ingest |
| 2026-09-17 | **Seed rows are exempt from the 14-day inactive rule**; their `last_seen` is the hand-recorded `captured_on` date, which is also the "price date" shown on the site. | Seed prices go stale, they do not disappear; pretending they were re-seen daily would make the "last updated" date a lie. | — | ingest, export |
| 2026-09-17 | **Export validated by tests against the code's own structure rather than a separate `schema.json`** (brief §15). Size guards: 2 MB per compound file, 5 MB total (resolves the §5.3 vs §15 mismatch). | Avoids a new dependency (jsonschema) and a second definition to keep in sync. Revisit if a bolt-on needs a published schema. | jsonschema + schema.json | export |
| 2026-09-17 | **Dedupe details:** key = brand + primary compound + form + pack units/type + multipack + rounded amount (brief §11 plus multipack). Product ids are kept stable by reading the previous run's matches before rebuilding. Overrides file shape: `merge: [{product, into}]`, `split: [listing_id]`. Offers for multi-ingredient products are exported under `combinations` (hidden until Phase 8), needs_review or unpriced under `unverified`. | Needed to implement §11/§9.5 step 9 | — | price, export |
| 2026-09-17 | **Fixes from reviewing the first real run row by row:** (a) the dedupe key now includes the product's other ingredients — H&B "Zinc 25mg" and "Chelated Zinc & Copper 25mg" had been falsely merged; key matching is done within a run and orphaned products are deleted, so a bad merge cannot persist; (b) natural constituents are accepted cofactors: omega-3 += phospholipids, choline, omega 6/9 (krill oil was wrongly a "combination"); L-theanine += green tea extract, EGCG (it is sold as a standardised green tea extract); (c) when the model gives no form, the code uses the form named in the title if exactly one class is named ("Vitamin D3 & K2" → d3; K2 stays unknown and unverified because no MK type is stated); (d) a compound that is neither the primary active nor named in the title is a minor extra (60 mg vitamin C in a zinc tablet) and is exported under `combinations`, not ranked. | Found by inspection; each has a regression test | — | `pipeline/price/build.py`, `rules.py`, `export/run.py`, `compounds.yml` |
| 2026-09-17 | **Evidence on elemental factors from real labels:** Myprotein states its bisglycinate at 11–11.25 % Mg, malate 14.7 %, taurate 7.2 %; our anhydrous factors are 14.1 %, 15.5 %, 8.9 %. Real raw materials are hydrated/buffered, so **estimates from compound mass run ~20 % high for glycinate**. No seed row currently relies on an estimate (all state elemental), so no price is affected. To decide before live feeds: lower the glycinate factor towards ~0.11–0.12 or widen the "estimated" caveat. | Honest uncertainty | — | `compounds.yml` magnesium factors |
| 2026-09-17 | **Site (Phase 4) decisions.** (a) `compounds.yml` gains a `classes:` block per compound — the label and URL slug for each comparison table (`mg_glycinate` → "Bisglycinate (glycinate)", `/c/magnesium/bisglycinate/`); the loader requires it to match the classes the forms use. (b) Compound URLs use hyphens (`/c/vitamin-d3/`); ids are unchanged. (c) Class pages build with ≥ 1 product for the prototype (brief §0 item 2); the launch thin-page rule of ≥ 2 (§21.2) is a one-line change in `site/src/lib/data.ts`. (d) Compound explainers and FAQs are **templated from the rules table**, not written per compound — factual by construction, no LLM calls, no health claims. The brief's optional two-sentence "evidence context" with NHS/NIH links is **not included** yet: sources could not be machine-verified and it is not needed for prototype-done. (e) "Your dose" rescales client-side from the pipeline's `price_per_unit`, as §12.2 specifies; state is in the URL only. (f) `claimed:` dietary chips are parsed in the export step from the listing text, with negations ("not suitable for vegetarians", "Non-Vegan") excluded. (g) Sitemap is generated only once `base_url` is set in `config/site.yml`. (h) A build-time smoke test (`site/scripts/smoke.mjs`, part of `make check`) asserts the bisglycinate page has ≥ 2 retailers, every page carries the disclosure and not-medical-advice lines, and no banned claim wording appears anywhere in the built HTML. (i) Product ids are now `brand-title` slugs; regenerated once pre-launch. | Brief §12, §16 | — | `site/`, `pipeline/export/run.py`, `config/compounds.yml` |
| 2026-09-17 | **Not done in Phase 4, deliberately:** Lighthouse was not measured (no Chrome tooling in this environment) — pages are static HTML with one small inline script and no images in tables, so ≥ 90 is expected; measure after first deploy. Search ranking for very short queries ("vit d") puts a product above the Vitamin D3 page — cosmetic, revisit with Pagefind ranking options. `/ops/` is Phase 5; `/learn/` is Phase 9. | Honest status | — | site |
| 2026-09-17 | **Visual direction: "Capsule" (Patrick's choice, "B2", from two rounds of mock-ups).** Clean and calm, with one recurring motif — a two-tone coral-and-teal capsule — used for the logo mark, the rank marker and the buy button (price in the coral half, retailer in the teal half), and nothing else decorated. Type: the Red Hat family (Display for headings and the per-dose price, Text for reading, Mono for figures), **self-hosted** via `@fontsource-variable/*` so the site makes no third-party requests (brief §16 privacy). Tokens in `site/src/styles/tokens.css`, light and dark. Rules: white text only on teal or ink — coral carries dark text (white on coral fails contrast); semantic colours stay separate from the motif; unverified sections use a quiet hatch, not a warning colour. Phones get card rows with three figure tiles (per dose largest); wide screens keep a table for side-by-side scanning. Now written into the brief as §12.4 (v1.5), flagged as needing a refinement pass before launch. Mock-ups: round 1 https://claude.ai/artifact/X88mfG35ckg42YYase3LRA, round 2 https://claude.ai/artifact/3drTS8s3qyhDBcwYAUVMyU, live preview https://claude.ai/artifact/KPqmUeT59TJuiCMq7BWWb7 (private to Patrick). Rejected directions: "Shelf Edge" (price-ticket, too loud), "Receipt", "Graduated" (ruler), "Label" (printed label). Patrick: "needs refining but that's the spec" — refine within this direction, do not change direction without asking. | Marketable end state; the plain scaffold had no identity | The four rejected directions | `site/src/styles/`, `OfferTable.astro`, `Base.astro`, OG images |
| 2026-09-17 | **Phase 5, the parts needing no accounts, built:** `/ops/` page + `ops.json`; `make review` → `make review-apply` round trip; regression guard (`pipeline/guard.py`, CLI `guard`) wired into both workflows. Details: (a) overrides file gains `exclude` and `values` alongside `merge`/`split`; person-confirmed `values` bypass the "must quote the label" rule for exactly the fields confirmed, and set confidence to 1.0; (b) the guard blocks when any table that had ranked products has none, or total products fall by more than half; first run passes; (c) `daily.yml` restores and saves the SQLite file through the Actions cache so cached AI readings and product ids carry between days (without it every listing would be re-read and paid for daily), plus a 30-day artifact backup (§18); (d) **the daily cron stays off until live feeds (Phase 6)** — seed CSVs do not change on their own; (e) **live golden runs are manual only** (`golden-live.yml`), not daily as §15 suggests. | (e) protects the API budget — Patrick asked to keep calls to a minimum; replay of saved readings runs on every push at no cost. | Daily live golden run (~40 calls/day) | workflows, `pipeline/review.py`, `pipeline/guard.py`, `pipeline/price/build.py` |
| 2026-09-17 | **Phase 5 acceptance not yet met, and why:** "review round trip proven on at least five real needs_review rows" — the seed data currently has **zero** unverified products (every label states its amounts), so the round trip is proven on fixtures only. Revisit when live feeds bring ambiguous listings. "A scheduled run completes and deploys without human action" needs Patrick's GitHub repo and Cloudflare Pages account. | Honest status | — | Phase 5 |
| 2026-09-17 | **Design refinement pass 1 + Phase 8 (combinations toggle), built while Patrick was away.** (a) Export gains `display_name`: the feed title minus " - <pack size>" segments and a repeated leading brand; `name` stays the raw title. Rows show the clean name with "brand · retailer" beneath, so the Retailer column is gone. (b) Home page: headline, search as the hero, one lead "from £X" figure per compound card with other forms listed beneath. (c) Phones: the cheapest card gets a teal top bar. (d) **Phase 8:** combination products render in the same table behind an "Include combination products (n)" toggle, off by default, state in the URL (`?combos=1`); they carry "Combination: price counts the {compound} only", never receive a "cheapest"/"best value" badge, and a class that has *only* combinations now gets a page (tab shows "(combinations)"). Any row with other actives shows an "also contains …" chip. Brief's Phase 8 check: ZMA-type products appear under magnesium and zinc only with the toggle on ✓; Myprotein's D3+K2 ranks under D3 with the chip ✓ but stays **unverified under K2** because its label never states MK-4 or MK-7 — correct behaviour, not a bug. Still open from the §12.4 refinement list: chips in row vs panel, share-card fonts, search ranking, long-form text identity. | Patrick: "build what you can while I am afk" | — | `site/`, `pipeline/export/run.py` |
| 2026-09-17 | **Not started without Patrick: Phase 6b registry expansion.** New compounds need ≥ 3 golden labels each, validated against the live model (API cost), and the brief requires Patrick to approve each batch. Adding them now would also fill `/ops/` with zero-product warnings until feeds exist. | Brief §17 Phase 6b; API budget | Draft offline now | — |
| 2026-09-17 | **Brief v1.6: end-state scope** — every reliable, reputable UK retailer covered and hundreds of supplements listed (Patrick). Reached through official affiliate feeds only; hand-collected seed data stays a small stopgap and is never scaled into scraping (§16 stands). A published "reliable and reputable" standard decides inclusion. | Patrick's instruction | — | brief §0, §7.35, `/ops/` |
| 2026-09-17 | **Standing autonomy grant (Patrick): complete five items without further input** — feed importers, more hand-collected retailers/products, a drafted (not live) next batch of compounds, learn-page safety tooling, design polish + seed refresh. **API budget: Claude proposed a £5 cap and Patrick replied "Complete those 5" without naming a figure, so £5 is treated as the ceiling**; spend is logged below as it happens. Anything outside the five items, any account/sign-up/payment, and anything over the cap waits for Patrick. | Patrick away; keep momentum | Free items only | all |
| 2026-09-17 | **Autonomous build, items 1, 3, 4 done.** (1) **Feed importer** (`pipeline/ingest/feeds.py`): Awin and Impact CSV (gzip auto-detected), columns mapped by name with per-retailer overrides, non-GBP and invalid rows counted and skipped, a missing required column or failed download skips that retailer only, feed URLs never printed; feed listings unseen for 14 days are hidden. Default Impact column names are from memory of the catalogue export and must be confirmed on signup. (4) **`make content-check`**: claim linter (`config/claims_banned.yml`) plus structure checks for learn pages — seven sections in order, 3–6 research cards each with a citation and a grade, labelled anecdotes with no quotes, users or brands, the fixed pharmacist/GP line, 600–900 words, approved pages need a named reviewer. `make content` (drafting) stays Phase 9. (3) **Drafted batch 1: 20 supplements, not live** (`config/drafts/`): B6, biotin, folate, calcium, iron, selenium, iodine, chromium, potassium, beta-alanine, glutamine, taurine, glycine, NAC, tyrosine, 5-HTP, CoQ10, glucosamine, MSM, caffeine — all expressible with existing normalisation types. 10 factors formula-checked; iron bisglycinate deliberately has **no** factor (commercial chelates ~20 % vs 27 % pure — the magnesium lesson). 60 synthetic golden labels, generated from a hand-written table (straightforward / awkward / combination per compound): rules 60/60, **live model 58/60 → 59/60 after a code fix**. Held back on purpose: citrulline (brief's ~66 % factor for 2:1 malate vs 72 % by chemistry — needs Patrick), choline, carnitine, vitamins A and E. | Patrick's standing grant | — | see files named |
| 2026-09-17 | **Rule added from the draft run:** when the label names no form and the model leaves `form_id` blank, the compound's catch-all form (the one whose names include `unspecified`) is used; a form the label *does* name but we cannot place still goes to review. Live data unchanged (71/71 ranked). | One of two draft misses; the other (a powder whose serving size the model did not capture) is correctly left unverified rather than guessed. | — | `rules.py` |
| 2026-09-17 | **API spend log (cap £5 from 2026-09-17 grant):** draft batch 1 live validation — 66 calls, est. £0.70. Running total under the grant: **~£0.70**. (Before the grant: two golden runs + 71 seed listings, ~167 calls, ~£1.60.) | Budget transparency | — | — |
| 2026-09-17 | **Autonomous build, item 2: Healthspan added as a fourth seed retailer** — 28 listings read page by page (provenance and the one-size-per-page rule in `data/seed/README.md`). 99 listings, 99 ranked, 0 needing review; every new row checked by hand against its page reading. One-time-purchase prices only, never subscription prices. Hand collection stops here: more retailers arrive through affiliate feeds (brief v1.6), not more page-reading. | Patrick's standing grant | Nature's Best, Simply Supplements (left for feeds) | `data/seed/healthspan.csv`, `config/retailers.yml` |
| 2026-09-17 | **API spend log:** Healthspan normalisation — 28 calls, est. £0.30, no escalations. Running total under the grant: **~£1.00 of £5**. | Budget transparency | — | — |
| 2026-09-17 | **Autonomous build, item 5 done.** (a) **Seed price refresh**: `make seed-refresh` writes a checklist of every seed listing (oldest first); `make seed-refresh-apply` writes checked prices/stock back into `data/seed/*.csv`, refuses typos (non-numbers, >3x change) and changes nothing on error. Only price, stock and capture date can change, so a refresh costs no AI calls. (b) **Design polish within Capsule (§12.4)**: the stock Pagefind widget is replaced by our own results list on the same index — comparisons first (a supplement's overview above its forms), then products, then site pages, each with one factual line instead of a text excerpt; long-form pages get a narrower reading column, a larger opening paragraph, ruled headings and the sums in the mono face; share cards are drawn as outlines of Red Hat Display (`opentype.js` + `@fontsource/red-hat-display`, build-time only) so they look the same on any build machine; claimed chips stay in the row. No new decoration was added (rule 1). | Patrick's standing grant; §12.4 refinement list | Keeping Pagefind's default UI (unstyled excerpts, alias text shown to users) | `pipeline/seed_refresh.py`, `site/src/pages/index.astro`, `global.css`, `og/*.png.ts`, brief §12.4 |
| 2026-09-17 | **Batch 1 approved by Patrick and made live:** the 20 drafted supplements moved into `config/compounds.yml` (30 compounds), their formulas into `config/factor_sources.yml`, 60 labels into the golden set (97 labels: rules 97/97, saved AI readings 96/97) and the draft files removed. No API calls: existing listings keep their cached readings. New supplements get pages only when a retailer's data contains them (none of the 99 seed products is one). Known gap: cached readings of existing listings list e.g. "Vitamin B6" as free text, so those products will not appear as B6 combinations until their label text changes or a forced re-read (~£1) is approved. | Patrick: "approve draft batch" | — | registry, golden set, tests |
| 2026-09-17 | **Magnesium bisglycinate factor 0.141 → 0.117** (Patrick asked for the recommended option). 0.117 is the dihydrate by formula (Mg(C2H4NO2)2·2H2O, 24.305 / 208.45) and sits next to what real labels state (11–11.25 %). The anhydrous figure made compound-only labels look ~20 % cheaper per dose than products that state elemental magnesium — an estimate should never flatter. Stated elemental still always wins; estimates stay marked "estimated". No current price changes (every seed row states elemental). Golden g02 now expects 58.5 mg. Malate and taurate are left as they are: one retailer's labels are thin evidence and no formula fits them better; revisit with feed data. | Evidence row above (real labels) | Remove the factor entirely (sends every compound-only glycinate label to review); keep 0.141 | `compounds.yml`, `factor_sources.yml`, brief Appendix A |
| 2026-09-17 | **Citrulline malate factor: chemistry, not the brief's ~66 %** (Patrick asked for the recommended option). 2:1 = 2 × 175.19 / (2 × 175.19 + 134.09) = **0.723**; 1:1 = **0.566**; ratio not stated → review. 66 % matches neither salt. Same method as every other factor (formula + atomic weights, test-checked). Citrulline itself is not live yet — it goes into draft batch 2, where the factor test needs a small extension (the active is a molecule, not an element). | Consistency with `factor_sources.yml` method | Keep 66 % | brief Appendix F, batch 2 |
| 2026-09-17 | **Hosting: Cloudflare Worker with static assets, not Cloudflare Pages.** Patrick's dashboard created a Worker project (Cloudflare's current default). Still free, still fully static — no server code. `wrangler.jsonc` holds the build command and publish folder so the dashboard needs no settings; `.node-version` pins Node 24 (Astro 7 needs ≥ 22.12); a 404 page was added. The Worker name `perdose` in `wrangler.jsonc` must match the dashboard project and changes with it if the site is renamed. Code pushed to github.com/patricklinsdell03-dev/perdose. | First deploy failed without a deploy config | Delete and recreate as a Pages project | brief §4 hosting, `wrangler.jsonc` |
| 2026-09-18 | **Domain: perdose.co.uk** (bought by Patrick via Cloudflare; he checked for clashing trade marks and found none). `base_url` set, which switches on canonical links, absolute share-image URLs and the sitemap. The name stays in `config/site.yml` only. | Needed for affiliate applications | per-dose.co.uk (hyphen: rejected) | `config/site.yml` |
| 2026-09-18 | **Affiliate networks.** Awin publisher account approved (ID 3098456, not a secret). At Patrick's explicit instruction, Claude searched the Awin directory in his Chrome and submitted join requests (ticking each programme's T&Cs on his say-so) to 14 UK programmes with feeds; Patrick had applied to 15 more himself. Full list in `docs/RETAILER_CANDIDATES.md`. Impact.com: partner account created, marketplace application declined (new site, no traffic) — retry later; a verification meta tag stays in the site header (no script). Holland & Barrett UK is not on Awin. | Brief v1.6 end state needs feeds | Tracking-script verification for Impact (rejected: third-party requests, §16) | retailers config (when feeds arrive) |
| 2026-09-18 | **Sellers shipping to the UK from abroad are included, behind a toggle** (Patrick: "filterable/toggleable"). Each retailer carries `ships_from` (ISO country, default GB) in `config/retailers.yml`; it travels on every exported offer and in `meta.retailers`. Comparison tables hide non-GB rows until the visitor ticks "Include sellers shipping from outside the UK" (URL `?intl=1`), and every headline figure — home cards, browse pages, page descriptions, share cards, the "cheapest" pick — uses UK-based sellers only. Non-GB rows carry a "ships from … — check delivery and duties" chip. Brief §7.3's UK-only wording is relaxed accordingly; the "reliable and reputable" standard still applies. First candidate: iHerb (GBP programme on Awin). | Patrick's instruction | Exclude entirely (brief v1.6 wording); include unmarked (misleading on delivery cost) | `ingest/run.py`, `export/run.py`, `data.ts`, `OfferTable.astro`, brief §7.3 |
| 2026-09-18 | **First live feed wired: Optimum Nutrition UK (Awin).** The feed arrived in Awin's newer Google-Shopping column format, so a third feed type `awin_google_csv` was added (price and currency share one cell, "22.00 EUR"). **All 809 rows are priced in EUR**, so ingest keeps 0 and reports `not_gbp 809` — honest, and the pipeline behaved as designed. Waiting on a GBP version of the feed from Awin's settings before any AI reads. `make ingest` now loads `.env` so feed URLs are found. | Real feed data | Convert EUR at a daily rate (rejected: prices must be what the shopper pays) | `feeds.py`, `retailers.yml` |
| 2026-09-18 | **One shared Awin feed, split by advertiser id.** Awin lets a publisher build a single feed over any advertisers (approved or not; only approved ones return products), so `.env` holds one `AWIN_FEED_URL` and each Awin retailer entry names its `advertiser_id`. The feed is downloaded once per run. Patrick's idea. | Fewer links to manage; approvals flow in without new config | One link per retailer (still supported) | `feeds.py`, `ingest/run.py`, `retailers.yml`, runbook |
| 2026-09-18 | **Draft batch 2: 20 supplements, not live** (`config/drafts/batch_02.yml`): thiamine, riboflavin, niacin (3 classes), inositol, copper, manganese, boron, **citrulline**, arginine, lysine, tryptophan, GABA, betaine (TMG), HMB, astaxanthin, berberine, quercetin, alpha-lipoic acid, chondroitin, hyaluronic acid. Factors by formula: copper gluconate 0.140, copper bisglycinate 0.300, citrulline malate 2:1 0.723 / 1:1 0.566, calcium HMB monohydrate 0.808; the factor test now handles actives that are molecules (`active_atoms`). 60 synthetic labels: rules 60/60; live model 52/60 first pass → **60/60** after three rule/label fixes below (71 calls, est. £0.75; grant total ~£1.75 of £5). Held back: vitamins A/E, choline, carnitine, NMN, collagen. | Patrick: "start it" | — | `config/drafts/`, `tests/golden/drafts/` |
| 2026-09-18 | **Rule: a form with `elemental_factor: 1.0` is the active itself** (pure L-citrulline) — a stated mass is used as-is with basis `stated_elemental`, never shown as an estimate. | Live run: the model marks "3g" of pure citrulline as the compound mass, which is correct, and there is nothing to estimate | A base form with no factor (sent every pure-citrulline label to review) | `rules.py` |
| 2026-09-18 | **Rule: `alias_exclusions` per compound** — phrases blanked out before candidate compounds are found, so "calcium HMB" and "calcium fructoborate" are not calcium products and "magnesium stearate" / "dicalcium phosphate" (excipients) do not make magnesium or calcium a candidate. Set on magnesium, calcium and potassium. **No `prompt_version` bump**: this only narrows the candidate list; no existing listing's reading is expected to change (the model already ignored excipients), and re-reading 99 listings + 97 golden labels would cost ~£2 for no benefit. Any future prompt bump re-reads everything anyway. | Live run: "Calcium HMB Powder" was read as a calcium product | Bump the version (rejected, cost) | `compounds.py`, `prompt.py`, `compounds.yml` |
| 2026-09-18 | **Boron: one class.** Calcium fructoborate does not get its own class yet — the model read it as calcium; labels state boron mg so it ranks correctly in the single boron table. Revisit if fructoborate products appear in feeds. | Live run | Two classes (brief F.2) | batch 2 |
| 2026-09-18 | **HMB follows the brief's `compound_default`:** "HMB 1000mg (calcium HMB)" is read as 1000 mg of the calcium salt → 808 mg HMB estimated. The model applied this consistently; the draft labels now expect it. Labels that say "providing X mg HMB" are still taken as stated. | Consistency with brief F.3 | Treat bare HMB numbers as the free acid | batch 2 |
| 2026-09-18 | `make golden-live ONLY=e14,e20` re-runs named labels only, so fixing a few misses costs pennies rather than a full batch. | Budget | — | `cli.py`, Makefile |
| 2026-09-18 | **Batch 2 approved by Patrick and made live:** 50 compounds in `config/compounds.yml`; golden set 157 labels (rules 157/157, saved AI readings 156/157); draft files removed. No API calls. New supplements get pages once a feed contains products for them. | Patrick: "Approve batch" | — | registry, golden set |
| 2026-09-18 | **Draft batch 3: 20 supplements, not live** (`config/drafts/batch_03.yml`): rhodiola, lion's mane, bacopa, maca, ginkgo, panax ginseng, saw palmetto, tongkat ali, elderberry, valerian, resveratrol, apple cider vinegar, psyllium, spirulina, chlorella, alpha-GPC, citicoline, huperzine A, lutein, L-leucine. No elemental factors. 60 synthetic labels: rules 60/60; live model 47/60 first pass → **60/60** after the two rule changes below (61 calls, est. £0.65; grant total ~£2.40 of £5). Still held back: curcumin, green tea, milk thistle, cranberry (dose is a constituent), vitamins A/E, choline, carnitine, beetroot. | Patrick: "Draft it" | — | `config/drafts/`, `tests/golden/drafts/` |
| 2026-09-18 | **Rule: for `extract_standardised` compounds a bare number is the mass of the product's form.** "Rhodiola 200mg" was read as `unclear` by the model and sent to review; for a herb the number is the extract or powder mass either way (the form decides the table), so it is now used at the reduced confidence cap that other unqualified numbers get. Live golden set unaffected. | 11 of 13 batch-3 misses | Keep sending them to review (would put most botanicals in the unverified bin) | `rules.py` |
| 2026-09-18 | **Rule: `compound_mass_is_dose` is now opt-in per compound** (on for creatine only) and **a stated content of the `standardisation_component` overrides the headline amount** for simple-mass compounds. "Alpha GPC 600mg (50%, providing 300mg)" and "Resveratrol 500mg extract (250mg trans-resveratrol)" now give 300 and 250; creatine's "4.4 g (from 5 g monohydrate)" still gives 5 g. | Batch-3 misses f32/f47 | — | `rules.py`, `compounds.py`, creatine entry |
| 2026-09-18 | **Batch 3 approved by Patrick and made live:** 70 compounds; golden set 217 labels (rules 217/217, saved AI readings 216/217). No API calls. | Patrick: "approve" | — | registry, golden set |
| 2026-09-18 | **Vitamins A and E: one IU conversion per compound, not per form** (Patrick: "don't do a per-form IU conversion, just use the base dose"). Brief Appendix F.1 wanted natural/synthetic vitamin E converted differently (1 IU = 0.67 mg d-alpha vs 0.45 mg dl-alpha) and retinol vs beta-carotene for A. Instead each compound gets a single `unit_conversions` entry and the label's stated mg/mcg is used as given; forms still get their own classes (d-alpha vs dl-alpha vs mixed; retinol vs beta-carotene) so like is compared with like. Where a label states only IU, the single factor applies and the basis is marked as converted. | Patrick's instruction; keeps the code generic (§9.5 unit conversion decision) | Per-form conversion tables (rejected: new code, few UK labels use IU for E) | batch 4, brief F.1 |
| 2026-09-18 | **Draft batch 4: 20 supplements, not live** (`config/drafts/batch_04.yml`): vitamins A, E (one IU factor each, per Patrick), K1, choline (bitartrate 0.411 by formula), carnitine (tartrate 0.682; ALCAR own class), curcumin, green tea, milk thistle, cranberry (all compared on a stated constituent), molybdenum, PQQ, nicotinamide riboside, lycopene, phosphatidylserine, MCT oil (C8+C10; pure C8 own class), cordyceps, reishi, ginger, garlic, tart cherry. 60 labels: rules 60/60; live model 52/60 → **60/60** after the rules below (67 calls, est. £0.70; grant total ~£3.10 of £5). Choline deliberately does not list alpha-GPC/citicoline as aliases (they are their own compounds; brief's "also under choline via factor" would double-list products — revisit). | Patrick: "draft it" | — | `config/drafts/` |
| 2026-09-18 | **Rules for constituent-compared compounds** (`constituent_required`): the dose is the stated constituent mass; failing that, its stated percentage × the stated extract mass; failing that, a headline figure *smaller* than the stated extract mass is taken as the "providing" figure ("36 mg PACs from 500 mg extract"). Basis `stated_constituent`. A label with none of these goes to review (`constituent_not_stated`). Forms marked `constituent_optional` (branded curcumin, turmeric powder, cranberry "extract" class) are compared on mass instead. A form marked `total_is_dose` (pure C8 oil) uses the stated total. | Batch-4 misses g16/g25/g44 | — | `rules.py`, `compounds.py`, export reason text |
| 2026-09-18 | Mushroom blends that name no form ("Cordyceps 500mg & Reishi 500mg") go to review as `form_unknown`, per brief F.6 ("state which; else review"). | Consistency | A catch-all form | batch 4 labels |
| 2026-09-18 | **Batch 4 approved by Patrick and made live:** 90 compounds; golden set 277 labels (rules 277/277, saved AI readings 276/277). No API calls. Batch-4 label ids were renamed g→h on merge because the prototype set already uses g01–g37 (the 37 original readings were restored from git; future batches use a fresh letter). | Patrick: "approve batch 4" | — | registry, golden set |
| 2026-09-18 | **Draft batch 5: 20 supplements, not live** (`config/drafts/batch_05.yml`): echinacea, chamomile, lemon balm, passionflower, hops, St John's wort, nettle (root/leaf), dandelion, holy basil, gotu kola, mucuna (by L-DOPA), kanna (Zembrin own class), shilajit (resin own class), fenugreek (Testofen own class), tribulus, horny goat weed, grape seed, pine bark (Pycnogenol own class), boswellia, moringa. 60 labels (ids j01–j60): rules 60/60; live model 59/60 → 60/60 after one label expectation was changed to "review" (61 calls, est. £0.65; grant total ~£3.75 of £5). | Patrick: "carry on with batch 5" | — | `config/drafts/` |
| 2026-09-18 | **A–Z lists every compound the pipeline knows**, with those that have no products yet unlinked and marked "awaiting retailer data", so growing coverage is visible before feeds arrive. Pages themselves still only build for compounds with products (brief §12.5). | Patrick could not see the batches on the live site | — | `a-z.astro` |
| 2026-09-18 | **Learn-page research comes from Europe PMC**, not the brief's OpenAlex / PubMed / Semantic Scholar trio. Europe PMC mirrors PubMed and returns each study's publication type, abstract and citation count in one free request with no key (limit ~10 requests/second per IP; `make content` makes 4 per supplement). OpenAlex now requires an account API key (free tier $1/day, checked on help.openalex.org today) - an account only Patrick can create; Semantic Scholar throttles keyless use. Citations on pages are PubMed ids, which `content-check` already accepts. Searches: titles must name the supplement, abstracts must mention supplements or oral use, last 15 years, English, meta-analyses/systematic reviews (most-cited per year, plus the 5 newest) then randomised trials, plus a few narrative reviews as background for "How it works". Titles about injections, animals, implants or crops are dropped before the AI sees them. | Free, no account, one request gives everything the rubric needs | OpenAlex with a key Patrick creates; PubMed E-utilities (no citation counts) | `pipeline/content/literature.py`, `config/content.yml`, brief §20.3 |
| 2026-09-18 | **Abstracts are never committed or published.** `content/<id>/evidence.json` keeps ids, titles, years, study types, citation counts and the AI's reading of each study (with short quotes); the abstracts themselves stay in the git-ignored cache `data/raw/literature/`. | NLM: PubMed abstracts may be under publishers' copyright, and the repo is public | Commit abstracts in evidence.json as brief §20.3 lists | `content/*/evidence.json` |
| 2026-09-18 | **Evidence grades: rubric PROPOSED by Claude, awaiting Patrick** (`config/content.yml` `grading`, `status: proposed`; `pipeline/content/grade.py`). A review (meta-analysis or systematic review) weighs as 3 trials; studies whose abstract is unclear are left out. Strong = ≥ 2 reviews, ≥ 1,000 participants in the largest review (or all trials together), ≥ 80 % pointing one way. Moderate = ≥ 1 review or ≥ 3 trials, ≥ 300 participants, ≥ two-thirds one way. Limited = two-thirds one way but smaller. Mixed = no direction reaches two-thirds. Insufficient = no review and < 2 trials. A grade says how settled a finding is **whichever way it points** (consistent reviews finding no clear difference grade Strong). No learn page may be approved until Patrick approves the rubric. | Brief §20.3: "the LLM proposes, the rubric decides" - the thresholds are a judgement call | Examine-style letter grades; GRADE (needs full-text risk-of-bias reading) | Phase 9 |
| 2026-09-18 | **`make content COMPOUND=<id>` built (Phase 9 drafting).** Two AI calls per page on the model in `config/llm.yml` `content` (Sonnet 5, Patrick's choice; effort `high`, the API default): (1) *reading* - for each study, relevant or not (by-mouth supplement use only; combinations, injections, diet-only studies out), topic, participants, what it found, each with the exact abstract words, checked in code like the label normaliser's quotes; (2) *writing* - the prose only. Code supplies the forms table (factors from compounds.yml, "per serving" ranges from our own price data), every card's grade line and source list, the fixed anecdote and pharmacist/GP lines, the compare-prices link and the frontmatter; citations of studies it was not given are removed and reported; 10-word runs copied from an abstract are reported. Topics are named by the outcome measured, never a disease ("Mood scores", not "Depression"). Up to 6 cards, the topics with the most evidence behind them. Answers are cached (`data/llm_cache/content/`), so re-assembling costs nothing. **Cost cap £0.50 per page** (`config/content.yml` `budget`), checked before any call on a generous estimate; magnesium estimates £0.19. `DRY=1` lists the studies and the estimate for free. A draft is saved as `review_status: draft` and never overwrites an approved page. | Brief §20.3; model extracts, code computes | One call writing the whole page including grades (rejected: the grade must come from the rubric) | `pipeline/content/`, `Makefile`, `config/llm.yml` |
| 2026-09-18 | **"What people report" is a fixed placeholder for now** ("No community discussions have been summarised for this page yet.") under the required anecdote header. Summarising threads Patrick curates (brief §20.3) is not built: it needs Patrick's time per page and a check of Reddit's current content terms first. | Keep Phase 9 to what needs no new permissions | Build thread synthesis now | learn pages |
| 2026-09-18 | **`make content-check` is now part of `make check`, and only APPROVED pages can fail it.** Drafts with problems are listed as DRAFT without failing the build (they are work in progress and are never published). The check also: counts words as a reader sees them (link addresses excluded); checks the 100-120-word `at_a_glance` excerpt for banned wording, brands and length; refuses citations of studies missing from the page's `evidence.json`; refuses any approved page while the grading rules are only proposed. It writes `data/export/learn.json`: the rubric wording plus every approved, passing page with a sha256 fingerprint of its text. The site builds a learn page only when it is listed there **and** the file still matches its fingerprint, because Cloudflare's build runs only the site build (no Python), so an edit after checking must not slip through. | Brief §20.4 "only approved pages build" enforced where the deploy actually happens | Port the checker to the site build (two copies of the rules) | `pipeline/content_check.py`, `Makefile`, `data/export/learn.json` |
| 2026-09-18 | **Learn pages on the site.** `/learn/<compound>/` is built from an Astro content collection whose loader reads only the pages in `data/export/learn.json` with a matching fingerprint (drafts never enter the build). Each page opens with a quiet note: what it is and what studies measured, not advice; drafted with the help of an AI model from the summaries of the studies cited, then checked by a person on <date> (the reviewer's name is kept in the file, not shown); grades follow fixed rules (link to the methodology). The compound page shows the page's 100-120-word "at a glance" excerpt **below** the price table (the table stays the first thing people see) with a link to the overview. The methodology page gains "How we grade research" (`#grading`) once a learn page is published. On phones a learn-page table keeps its headings and scrolls sideways inside itself instead of turning into price cards. The smoke test fails if any learn page is built that is not in the manifest. Verified with a throwaway fixture page built outside the repo (desktop and 375 px): no sideways page scroll, all seven sections, grades and links present. | Brief §12.1, §20 | Excerpt above the table | `site/src/content.config.ts`, `site/src/pages/learn/`, `c/[compound]/index.astro`, `methodology.astro`, `global.css`, `scripts/smoke.mjs` |
| 2026-09-18 | **Learn pages are drafted inside Claude Code sessions (Patrick's subscription), not via `make content` API calls** (Patrick: "no need to use API tokens"). `make content` stays as an optional tool; the normal path is: fetch studies from Europe PMC (free), Claude reads and writes the page and `evidence.json` in the session, `make content-check` gates it, Patrick approves. The grading rubric in `config/content.yml` still needs Patrick's approval before any page can be approved. | Zero API cost | `make content` per page (~19p) | Phase 9 workflow, HANDOVER §4 |
| 2026-09-18 | **Amazon: planned via the Product Advertising API (brief §19 item 4), not excluded.** `docs/RETAILER_CANDIDATES.md` had it as "excluded (marketplace)" — corrected. Sequence: Patrick joins Amazon Associates UK → "Also on Amazon" affiliate search links on product pages earn the 3 qualifying sales the API requires within 180 days → adapter built → Amazon listings join the tables under the same unverified-if-ambiguous rule. | Patrick asked how/when | Skip Amazon | Phase 7 |
| 2026-09-18 | **Wider retailer coverage = more networks.** Beyond Awin: Webgains, Rakuten Advertising, CJ, Partnerize, Tradedoubler (all free for publishers). Patrick signs up; Claude searches each directory in his Chrome on his say-so; feeds map by column name (`column_map`), so most are config only. | Brief v1.6 end state | Awin only | `docs/RETAILER_CANDIDATES.md` |
| 2026-09-18 | **Brief v1.8 (Patrick): completeness is the point, and Amazon is a core source.** Every reputable UK seller is the target; refusals are named on the methodology page, not hidden. Amazon joins every table via the Product Advertising API at the Buy Box price (one row per product, daily refresh) once Associates qualifies; the adapter is built now against Amazon's published format on fixtures. Pending decision for Patrick: the seller-quality line for third-party Amazon sellers (proposal: Amazon-fulfilled or high seller rating). | Patrick's push-back on 2026-09-18 | Amazon as a side link (rejected) | brief §0, §7, §19 |
| 2026-09-18 | **Basket builder with "one retailer" and "delivered within N days" filters** (Patrick) — recorded in brief v1.8 as the shape of the stack calculator (§19 item 8). Needs `delivery_days` per retailer in `config/retailers.yml` and delivery cost in the basket total. Scheduled after the first few live feeds (Phase 7), because with four retailers it has nothing to choose between. | Patrick's instruction | — | brief §19, Phase 7 |
| 2026-09-18 | **Amazon adapter built** (`pipeline/ingest/amazon.py`, feed type `amazon_paapi`, retailer `amazon` disabled until credentials exist): PA-API 5 SearchItems per compound name (3 pages, 1 request/s), one listing per ASIN at the Buy Box price, GBP only, seller rule = Amazon / Amazon-fulfilled / rating ≥ 4.5 with ≥ 100 ratings (Patrick to confirm the line), SigV4 signing. Tested on a fixture in Amazon's documented response shape; the live call is untested until access is granted, so the first real run is a supervised one. Label text comes from the listing's feature bullets (PA-API 5 has no description field), so expect more "unverified" than a retailer feed gives. | Brief v1.8 | Third-party SDK (rejected: one more dependency for one endpoint) | `amazon.py`, `retailers.yml`, runbook |
| 2026-09-18 | **Brief v1.9 (Patrick): home page as control surface; "Build a basket" tab with a max-retailers setting.** Basket cost = for each retailer subset of size ≤ X, the cheapest offer per item at the user's dose (packs bought whole, cost over the chosen period) plus that retailer's delivery charge from `retailers.yml`; the cheapest subset wins. Delivery-days filter waits for `delivery_days` data. | Patrick's instruction | — | `site/src/pages/basket.astro`, `index.astro`, `OfferTable.astro` |
| 2026-09-18 | **Built: home quick-compare and `/basket/`** (brief v1.9). Table filters (retailers, in-stock, tested) are now URL state alongside dose/combos/intl, so the home strip and shared links set them. Basket: whole packs to cover dose × days, cheapest offer per item within each retailer subset of size ≤ X, delivery from `retailers.yml` ("not known" costs £0 and is labelled — Bulk is the only seed retailer with a stated under-threshold fee; Patrick to supply the others'), shareable `?items=…&x=`. Data comes from `/basket-data.json`, generated from the same export as the tables. | Patrick's instruction | Server-side optimiser (rejected: core stays static, §22) | `basket.astro`, `basket-data.json.ts`, `index.astro`, `OfferTable.astro`, `Base.astro` nav |
| 2026-09-18 | **Amazon Associates live (tag `perdose-21`, in `config/site.yml`, not a secret).** Interim "Check on Amazon (price not compared)" search links on every table row and product page — quiet text, never a buy capsule, so the cheapest compared price stays the default; Amazon's required "As an Amazon Associate…" line is in the footer and the disclosure page. These links exist to earn the 3 qualifying sales that unlock the Product Advertising API; once Amazon rows carry real prices they are ranked like any retailer (Patrick: Amazon is the default only when it is cheapest, differences must be visible — the per-dose ranking does exactly that) and the search links go. | Patrick's instruction | Amazon buy button without a price (rejected: misleading) | `site.yml`, `site.ts`, `OfferTable.astro`, `p/[product].astro`, disclosure, footer |
| 2026-09-18 | **Multivitamins added (Patrick) — first `per_serving` compound** (brief F.8 had it as Phase 10). Unit `serving`, standard dose 1: the price per standard dose is the price per daily serving; a multi-nutrient product is not a "combination" of itself, but it still appears behind the combinations toggle in single-ingredient tables it contains. Multivitamin terms removed from the ingest exclusion list (golden negative n03 retired). One table; men's/women's/kids' show in the product name. Three golden labels (mv01–03): rules 280/280, live model 3/3 (4 calls, ~4p). **Follow-up:** a side-by-side nutrient grid per serving is the table this class really needs; today the row shows the list of other actives only. | Patrick: "Add multivitamins to the site" | Wait for Phase 10 | `compounds.py`, `rules.py`, `format.ts`, `ingest.yml`, golden set |
| 2026-09-18 | **Combinations section built** (`/combinations/`, Patrick): every product carrying ≥ 2 of our compounds, or any multivitamin, filtered by "a daily serving must contain at least X of each" (in each compound's unit), sorted by price per daily serving. Presets: ZMA, D3+K2, Calcium+D3, Magnesium+B6, Vitamin C+zinc, B-complex, Iron+vitamin C, Omega-3+D3, any multivitamin. State in the URL (`?need=zinc:10,magnesium:300&multi=1`). Data from `/combos-data.json` (same export as the tables). **Known gap:** the 99 seed listings were read before batch 1 went live, so their B6/iron/etc. are free text rather than measured actives — the ZMA preset finds nothing until those listings are re-read (~£1, needs Patrick's yes) or feeds replace them. | Patrick's instruction | — | `combinations.astro`, `combos-data.json.ts`, nav |
| 2026-09-18 | **Combinations presets: the ten most common UK combinations first** (Patrick: "add the top 10"): ZMA; D3+K2; calcium, magnesium & zinc; glucosamine & chondroitin; vitamin C + zinc; vitamin C, D3 & zinc; B-complex; hair, skin & nails (biotin, zinc, selenium); folic acid + B12; caffeine + L-theanine — then calcium + D3, magnesium + B6, iron + vitamin C, omega-3 + D3, any multivitamin. Preset amounts are low "at least" filters, labelled on the page as starting filters, not recommendations (hard rule 4). Chosen from what UK retailers shelve as combination products; no sales data behind the order. | Patrick's instruction | — | `combinations.astro` |
| 2026-09-18 | **Re-read all 99 seed listings against the 91-compound registry** (Patrick: "reread them"). 102 calls, est. £1.05. **Grant total ~£4.85 of £5** — the next paid step needs Patrick's go-ahead. Result: 31 new combination rows (B-vitamins, B6, copper, choline, astaxanthin…) so the Combinations presets find products; 7 products were wrongly pushed into "combination" by fillers until the rule below; B12 at 0.63 µg in two Healthspan magnesium products is no longer recorded (the label names more than the 8-candidate cap), which affects no comparison. Regression guard ok. | Combinations needed measured secondary actives | Wait for feeds | seed readings, export |
| 2026-09-18 | **Rule: an ingredient-list mention with no amount and no mention in the title is a filler, not an active** (calcium carbonate bulking agent, MCT glazing agent, tocopherol antioxidant, lecithin liposomes). Found by the re-read: with 91 compounds known, the model listed fillers as actives. Tocopherols also added to omega-3's accepted cofactors (antioxidant in fish/algae oils). Golden set unchanged (280/280 rules, 279/280 replay). | Re-read results | Per-compound alias exclusions for every filler (rejected: endless list) | `rules.py`, omega-3 entry |
| 2026-09-18 | **Combination presets find the combination, the user sets the amounts**: ZMA preset lowered to zinc ≥ 5 mg, magnesium ≥ 100 mg, B6 ≥ 1 mg (was 10/300/5, which excluded every zinc-magnesium-B6 product we list). | Presets should find products | — | `combinations.astro` |
| 2026-09-18 | **Budget raised to £10 (Patrick: "quick stopgap with 10 budget"). Multivitamin stopgap: 19 hand-read listings** (16 multivitamins, 3 combinations) from Healthspan, H&B and Bulk; details in `data/seed/README.md`. 27 calls, est. £0.30 → **grant total ~£5.15 of £10.** Multivitamin aliases gained product-line names (MultiVitality, Ultra Man/Woman, multimineral) because titles often never say "multivitamin"; the six affected readings were cleared and re-read. H&B under-£30 delivery £3.49 recorded (from its product pages' shipping data), so baskets price H&B delivery exactly. | Patrick | Wait for feeds | seed CSVs, `retailers.yml`, multivitamin aliases |
| 2026-09-18 | **Collagen added (Patrick: "where is collagen") as `simple_mass`, price per 10 g of collagen**, not the brief's `per_gram_macro` (collagen labels state the collagen mass per serving, which the existing type handles, including g→mg). Classes: bovine, marine, unspecified source, type II/UC-II (own table, standard dose 40 mg). "Collagen builder/booster" and "vegan collagen" are excluded as not collagen (brief F.3). 6 hand-read listings (5 from H&B incl. Myprotein and Optimum Nutrition products it sells, 1 Healthspan UC-II); 3 golden labels (cl01–03, rules and live 3/3). Optimum Nutrition's powder is unverified: its label gives protein per serving, not collagen, and no pack-servings — correct by the ambiguous-is-unverified rule. 9 calls, ~10p → grant ~£5.25 of £10. | Patrick | Wait for Phase 10 | registry, seed CSVs, golden set |
| 2026-09-19 | **Light Capsule design is the default on every device; dark mode only on request.** Patrick saw the dark palette on his phone (system dark mode) and did not recognise it as the new design. `tokens.css` dark block moved from `prefers-color-scheme` to `:root[data-theme='dark']`; `<meta name="color-scheme" content="light">`; a "Switch to dark mode" button in the footer remembers the choice in the visitor's browser (localStorage; falls back to light). Brief §12.4's "designed dark mode" is kept, as an opt-in. | Patrick: "the new design was white" | Keep following the system setting | `tokens.css`, `Base.astro` |
| 2026-09-19 | **"Show price" switch on every table: per dose / week / month / year** (Patrick). The main (largest) figure becomes the cost over that period at the visitor's daily dose (week = 7 days, month = 30 as before, year = 365); the second tile shows per month, or the per-dose price when the main tile already shows the month. Ranking is unchanged (same order at a fixed dose). State in the URL (`?per=year`). Brief §12.4 rule 3 ("the per-dose price is the largest figure") now reads: the chosen price is the largest figure; per dose is the default. | Patrick's instruction | — | `OfferTable.astro`, `global.css` |
| 2026-09-19 | **Home and browse supplement cards now show the cheapest product as a full Capsule card** (rank capsule, price-per-dose tile, buy capsule, working folded) instead of a tinted "From £X" box (Patrick: "all of these need to be in the capsule theme box"). Other forms' prices and the product/retailer counts stay underneath, plus "See all N prices". Uses the same `OfferRow` as the tables, so figures can never disagree. | Patrick | — | `CompoundCard.astro`, `global.css` |
| 2026-09-19 | **Home page starts generic** (Patrick): the magnesium-bisglycinate example card beside the opening is removed; the opening is headline, search, quick compare and facts, then every supplement's Capsule card. **"Show price" (per dose/week/month/year) added to the home quick-compare row**: it rescales every card's figures live (one standard dose a day) and carries `?per=` to the table the visitor opens. | Patrick | Keep one featured product | `index.astro`, `CompoundCard.astro`, `OfferRow.astro` (`data-std`), `global.css` |
| 2026-09-17 | Python project and npm package are named `pipeline` and `site`, not the working site name. | CLAUDE.md: the name lives only in `config/site.yml`. (`data/perdose.sqlite` and `PERDOSE_LIVE_LLM` are kept because the brief names them.) | — | `pyproject.toml`, `site/package.json` |
| 2026-09-19 | **FSA/GB novel-food check on 5 candidate supplements (this run, before batches 6-8 merge): 4 unauthorised, 1 unclear.** Checked against the EU Novel Food Status Catalogue (inherited into GB law) and the FSA's own GB Register of Novel Food Authorisations (data.food.gov.uk, current to 2026-03-31), plus the GB regulated-product application tracker and enforcement/trade press. **NMN** — catalogue entry (updated 2023-10-18) rules it novel; GB tracker shows a pending EffePharm application (RP-2116, "risk assessment – in progress"); not on the GB authorisations register; EFSA gave a positive *safety opinion* 2026-05-11 but that is a preliminary EU step, not authorisation, and is not GB law. **Lithium orotate** — the sole EU authorisation attempt (NF/2024/30250) was formally terminated without authorisation on 2026-03-27; independently, lithium is not on the permitted-minerals list in Schedule 1 to the Food Supplements (England) Regulations 2003 (SI 2003/1387) at all. **Agmatine (sulfate)** — catalogue entry (2023-10-17) rules it novel; not on the GB register; RASFF border-rejection notices continuing into 2021 and a 2016 Malta public warning confirm active enforcement history. **DMAE** — catalogue entry (updated 2025-02-07) rules it novel; not on the GB register; a 2023 peer-reviewed review independently lists it as unauthorised; a 2025-05 trade note shows industry still trying to overturn the classification, i.e. unresolved. **Uridine (monophosphate)** — no entry either direction in the EU catalogue and absent from the GB register; absence is ambiguous (could be "never needed assessing" or "novel but unflagged") and no enforcement action either way was found — kept as **held/unclear** rather than waved through, per this run's own conservative instruction. **Decision: agmatine (batch 6), DMAE and uridine (batch 7), NMN and lithium orotate (batch 8) are held out of their batches and not merged** — each batch's compounds.yml/golden-label drafting went ahead regardless so the work is ready, but the merge step excludes these five. Exact questions for Patrick are in the final report. | Hard requirement for this run: "not permitted, or an unauthorised novel food = hold"; four of five have direct, dated, primary-source confirmation, the fifth has no official determination either way | Ship them and hold only on a stop-sale notice (rejected: contradicts the "hold" instruction, and the RASFF/enforcement history for agmatine shows this is already actively policed); treat "uridine has no red flag" as lawful (rejected: absence of enforcement isn't evidence of legality, and the instruction's asymmetry favours caution) | `config/drafts/batch_06.yml` (agmatine), `config/drafts/batch_07.yml` (dmae, uridine), batch 8 (NMN, lithium orotate — not yet drafted at the time of this entry) |
| 2026-09-19 | **Batch 5 audit (two rounds, independent subagents): 13 PASS, 6 FIX, 1 HOLD.** Round 1 found: (a) three one-directional `accepted_cofactors` gaps tied to specific golden labels — live `ashwagandha` gained "holy basil" (golden j27), live `bacopa` gained "gotu kola" (was empty; golden j30), live `curcumin` gained "boswellia" (golden j57); batch-5 `chamomile` gained "hops" and live `valerian` gained "chamomile" (not golden-tested, fixed for consistency); j27/j30/j57 each gained an explicit `multi_ingredient: false` assertion they were missing, closing the exact gap that let the asymmetry go unnoticed; (b) boswellia's alias "frankincense" collides with aromatherapy/essential-oil/incense products at the same retailers — fixed with an `alias_exclusions` entry; (c) the "THR-registered products only" note on echinacea/st_johns_wort has no schema/extraction backing yet — accepted as a known, non-blocking limitation (the design, a label flag rather than a hard exclusion, is still the correct MHRA-registration-scheme call); (d) **kanna held** — unresolved doubt on GB Novel Food status (no clear pre-1997 EU food-use history) and, separately, whether the Psychoactive Substances Act 2016's exemptions clearly cover it; not one of the five compounds this run's dedicated FSA catalogue check covered (that covered agmatine/DMAE/uridine/NMN/lithium orotate only), so treated as its own, lower-confidence hold rather than guessed either way. Round 2 verified every fix directly against the files and found no new issue and no collision; shilajit was reconsidered and confirmed not needing a hold (no separate psychoactive-substance angle, unlike kanna, and current open UK retail availability — though it wasn't put through the same primary-source catalogue check, flagged as a reasonable follow-up, not a blocker). Kanna's compound entry and its three golden labels (j34-j36) stay in `config/drafts/batch_05.yml`/`tests/golden/drafts/batch_05.yml`, excluded from this batch's merge. | Patrick's audit requirement this run | — | `config/compounds.yml` (ashwagandha, bacopa, curcumin, valerian), `config/drafts/batch_05.yml`, golden set |
| 2026-09-19 | **Batch 8 audit: 2 PASS, 1 FIX, 2 HOLD independently re-verified.** Fix: live `electrolytes` needed an `alias_exclusions` entry (`[dioralyte, oral rehydration therapy, oral rehydration salts, oral rehydration solution]`) — the auditor found, by searching Boots' actual site, that Dioralyte (a licensed pharmacy medicine for rehydration during illness) is sold there, and because `per_serving` compounds need no stated amount at all to become rank-eligible (only resolvable pack/serving counts), an ORT medicine could have become a fully-priced row indistinguishable from a sports hydration product — nothing about it would have looked ambiguous to the pipeline. No live feed carries a pharmacy retailer yet so this had no live effect, but the guard is in now, ahead of one. `lithium_orotate`'s factor calculation was independently re-derived from scratch by the auditor (monohydrate 3.853%, dihydrate 3.504%) and matched the draft's own numbers exactly. Minor, non-blocking name-collision notes: `colostrum` shares its name with a real H&B skincare line ("24/7 Colostrum Pore Defence Ampoule") and `dextrose`/`maltodextrin` also appear in mass-gainer products (F.11-excluded) — both confirmed contained by the existing needs_review/combinations-toggle mechanisms rather than needing an immediate fix. The auditor also made a fair, precise correction to this log's own framing: the "live AI on the batch's labels" bar is cleared under *either* reading (9/9 for the three merging compounds, or 14/15 for the whole batch including both held compounds) — it isn't a case that needed the narrower reading to pass, as an earlier entry implied. Separately, `n06` (a lithium_orotate combination label) was rewritten to match the model's actual, already-cached answer for it (ambiguous_basis, not the clean elemental reading originally guessed) after the credit exhaustion made re-verifying the guess impossible — this was needed to keep the automated per-draft 90% threshold test (`test_drafts.py`) green, and is a more honest label regardless: it now asserts only what real cached model output proved, not what a hoped-for fix would have produced. On the credit-exhaustion question itself, the auditor's honest view: the numeric bar is clearly cleared either way, and lithium_orotate being held on legal grounds is unaffected by its own label-testing gap either way — but flagged, fairly, that this is the first batch this run where a hold was finalised with an unresolved live-recheck gap (batches 6 and 7's held compounds both reached a clean, fully re-verified 100% first), and that whether an unplanned infrastructure failure mid-run should itself trigger a stricter pause is a judgement call the standing delegation didn't explicitly anticipate — surfaced here rather than assumed away, and repeated in the final report to Patrick. Batch 8 merged: colostrum, dextrose_carbs (3 classes), electrolytes; nmn and lithium_orotate stay held. Golden set 373 labels (364 + 9); calc-only 373/373, replay 372/373 (the same pre-existing, unrelated `d28`). | Patrick's audit requirement this run | — | `config/compounds.yml` (electrolytes), `config/drafts/batch_08.yml`, golden set |
| 2026-09-19 | **Draft batch 6: 15 supplements, not live** (`config/drafts/batch_06.yml`): vitamin B5, BCAA, EAA, ornithine, betaine HCl, agmatine, sodium bicarbonate, glycerol, CLA, beetroot/nitrate, flaxseed oil (ALA), evening primrose (GLA), black seed oil, silica (3 classes: bamboo/horsetail/orthosilicic, no factors — commercial material varies too widely, same reasoning as iron bisglycinate), zinc carnosine. 45 synthetic golden labels (k01-k45): rules 45/45; **live model 42/45 (93%) first pass → 45/45 (100%) after three fixes**, all logged below. Two live-registry `alias_exclusions` additions (name-collision prevention, precedented pattern): `betaine` gained `[betaine hcl, betaine hydrochloride]`, `zinc` gained `[zinc carnosine, zinc-carnosine, zinc l-carnosine, pepzin]` — confirmed necessary, not precautionary: without the zinc fix, "Zinc Carnosine (PepZin GI) 75mg" was read as a **zinc** product (wrong compound, wrong price basis) by the live model, exactly the collision this batch was designed to avoid. `sodium_bicarbonate`'s aliases were too narrow (missed "Bicarb Loading" with no "sodium" prefix) — added bare "bicarb"/"bicarbonate". One golden label (k42, horsetail-extract silica) was corrected: a herbal-extract-labelled silica source reads as `amount_refers_to: extract`, which `mineral_elemental` correctly has no case for and sends to review as `ambiguous_basis` rather than `amount_missing` as originally (wrongly) written — the test was fixed to match this real, safe behaviour, not the other way round. **agmatine is held** (unauthorised GB novel food, see the FSA-check entry above) — fully drafted and validated so the work is ready, but excluded from this batch's merge. 47 live calls total (45 + 2 to re-check the two fixed misses with `ONLY=`), est. £0.47. | Patrick's delegation this run | — | `config/drafts/batch_06.yml`, `config/compounds.yml` (betaine, zinc), golden set |
| 2026-09-19 | **Draft batch 7: 10 supplements, not live** (`config/drafts/batch_07.yml`): chaga, American ginseng, glucomannan, inulin, PHGG, sea moss, black pepper (piperine), beta-glucan. 30 golden labels (m01-m30): rules 30/30; **live model 28/30 (93%) first pass → 30/30 (100%)** after fixing two labels whose *phrasing* (not the rules) was ambiguous — m11's "1 tsp (4g) Serving" led the model to read units_per_serving as 1 instead of 4 (simplified to a plain "Serving Size 4g"); m20's "10mg (95% Piperine, 9.5mg)" wasn't parsed as a separate piperine figure (reworded to "10mg (providing 9.5mg Piperine)", matching the working curcumin pattern). m03 (Chaga+Reishi, one stating "30% Beta-Glucans") deliberately stress-tests the beta-glucan/mushroom-standardisation collision risk named in the run brief — the live model correctly did not invent a separate beta_glucan active; **PASS first time**, no fix needed. **dmae and uridine are held** (DMAE: unauthorised GB novel food; uridine: no official determination found either way — kept blocked rather than waved through, per the FSA-check entry above). 32 live calls total (30 + 2 to re-check the two fixed misses with `ONLY=`), est. £0.32. | Patrick's delegation this run | — | `config/drafts/batch_07.yml`, golden set |
| 2026-09-19 | **Final spend total, this run.** Recounting from each `make golden-live` run's own reported `API calls:` figure (more accurate than the running total logged incrementally through the run, which drifted a little low): batch 6 = 45 + 2 (re-check) = 47 calls; batch 7 = 31 + 2 + 1 (two re-check rounds) = 34 calls; batch 8 = 17 calls (before the account's credit ran out; no further calls possible after that). **Total this run: 98 calls, est. £0.98 — under the £2.00 authorised for this run.** Running total: **~£6.23 of the £10 cap** (previously ~£5.25). | Budget transparency; correcting drift in the incrementally-logged running total against the actual per-run call counts | — | — |
| 2026-09-19 | **Batch 6 audit: 9 PASS, 5 FIX, 1 HOLD — the most substantive audit this run.** Two real, previously-undetected name collisions, both found by the auditor directly diffing every new alias against the full live registry (not just spot-checking): `sodium_bicarbonate`'s widened aliases (`bicarbonate`/`bicarb`, added after the k20 live-validation fix) collided with live `potassium`'s own pre-existing `bicarbonate` form (config/compounds.yml's potassium entry) — fixed with `alias_exclusions: [potassium bicarbonate]`; `flaxseed_oil`'s bare `ala` alias collided exactly with live `alpha_lipoic_acid`'s own pre-existing bare `ala` alias (alpha-lipoic acid vs alpha-linolenic acid genuinely share the abbreviation) — fixed by dropping it (the other four aliases already cover real flaxseed listings). One genuine design flaw, proven empirically rather than argued in the abstract: `silica`'s `label_convention: elemental_default` was checked by the auditor against the real saved live-model cache for k41 ("Choline-Stabilised Orthosilicic Acid 10mg" — about as clean a label as possible) and the model still returned `amount_refers_to: unclear` for it; under `elemental_default` an unclear reading is silently ranked as elemental regardless of source, which for bamboo/horsetail extract would be exactly the flattering, unfounded estimate this compound's own design says it wants to avoid, and the opposite of Appendix F.2's own "often unclear; expect review". Fixed: `label_convention: ambiguous` plus a `heuristics.unclear_amount` block (`compound_forms: [bamboo, horsetail], compound_if_at_least: 0, elemental_if_at_most: 50`), the same mechanism already used for magnesium — bamboo/horsetail now never rank on an unclear reading regardless of size, while orthosilicic acid's genuinely low real-world doses (~5-10 mg) still do. Two labels (k46, k47) were added to cover the fix and a missing combination case (silica accepted "biotin" as a cofactor, matching the live collagen precedent for Appendix F.9 hair/skin/nails bundling) — verified calc-only and via replay of the *existing* k40-k42 cache (confirming the fix doesn't regress what was already live-validated), but **could not be live-validated themselves: the account's API credit ran out right after this fix was written** (see the credit-exhaustion entry above, which was originally about batch 8 but the timing means it affects this fix too). k46/k47 are held back from the live golden set (`tests/golden/labels.yml` requires every listed label to have a cached reading — `tests/test_golden.py`'s completeness check caught this correctly) and kept in `config/drafts/batch_06.yml`/`tests/golden/drafts/batch_06.yml` as a clearly marked "pending live validation" block, next to (but distinct from) agmatine's hold; silica already has 3 live-validated labels (k40-k42) without them, so this is a confidence top-up to run once credit is restored, not a blocker. Two documentation-only fixes: the bcaa/eaa notes claimed a secondary active named with its own amount (e.g. "Leucine 2500mg" inside a BCAA blend) "will also get its own row" in its standalone table — the auditor executed the rules against the real cached k05/k08 extractions and found this is only true when the primary's own total is *also* cleanly stated (needs_review is a product-level flag, so an unstated BCAA/EAA total suppresses the otherwise-clean secondary reading too); notes corrected to state the actual precondition. Batch 6 merged: 14 compounds (vitamin_b5, bcaa, eaa, ornithine, betaine_hcl, sodium_bicarbonate, glycerol, cla, beetroot_nitrate, flaxseed_oil, evening_primrose, black_seed_oil, silica, zinc_carnosine); agmatine stays held. Golden set 415 labels (373 + 44, minus the 2 pending); calc-only 415/415, replay 414/415 (the same pre-existing, unrelated `d28`). | Patrick's audit requirement this run | — | `config/compounds.yml` (sodium_bicarbonate, flaxseed_oil, silica), `config/drafts/batch_06.yml`, golden set |
| 2026-09-19 | **Batch 7 audit: 7 PASS, 1 FIX, 2 HOLD confirmed sound.** Fix: the live `panax_ginseng` compound needed an `alias_exclusions` entry (`[american ginseng, panax quinquefolius]`) — it already has bare "ginseng" as an alias, which would otherwise also offer it as a candidate on every american_ginseng listing; the model happened not to act on the collision in this batch's own three american_ginseng labels, but the auditor traced the candidate-selection code and confirmed the risk was real, not theoretical, so the fix stands regardless of the labels passing. Also logged (as a form note) why `american_ginseng` splits into two classes (extract/powder) although Appendix F says "one class" for it — mirrors the existing live `panax_ginseng` precedent, which does the same despite the same brief wording. Two disclosed, non-blocking architecture gaps the auditor traced in code rather than just asserted: (a) a percentage-only "beta-glucan" mention inside a mushroom product's components can never override that mushroom's own dose (`_component()` filters out percent-unit entries) — but a *phantom* `beta_glucan` active being invented for such a mention isn't structurally prevented by anything, only shown not to happen on the one label tested; worth watching the beta_glucan "unverified" bin once real mushroom feed volume arrives; (b) confirmed again (as with batch 5's boswellia/curcumin finding) that Appendix F's "ranked only when sold alone" has no enforcement anywhere in the rules engine — black_pepper will rank inside existing curcumin/ashwagandha+piperine combos at their small piperine dose, same as every other accepted-cofactor pair. Batch 7 merged: 8 compounds (chaga, american_ginseng, glucomannan, inulin, fibre_phgg, sea_moss, black_pepper, beta_glucan); dmae and uridine stay held. Golden set 364 labels (340 + 24); calc-only 364/364, replay 363/364 (the one miss is the same pre-existing, batch-unrelated `d28` the batch-5 audit already found). | Patrick's audit requirement this run | — | `config/compounds.yml` (panax_ginseng), `config/drafts/batch_07.yml`, golden set |
| 2026-09-19 | **The Anthropic account's real credit balance ran out mid-run, independent of the £2.00/£10 spending authorisation this run was tracking against.** After ~96 live calls total this run (batch 6: 47; batch 7: 32; batch 8 first pass: 17 — an estimated ~£0.96, still well inside the £2.00 authorised for this run), a call for batch 8 returned `anthropic.BadRequestError: "Your credit balance is too low to access the Anthropic API."` This is the account's actual balance, a separate thing from the £10 running-total figure this log has been tracking (that figure is Patrick's self-imposed spending ceiling, not a live balance check) — the two had drifted apart before this run started, and this run's own tracking never caught up to the real constraint because nothing checks it directly. **Consequence: two of batch 8's 15 golden labels (n05, n06 — both `lithium_orotate`, already held on legal grounds) have fixes applied and verified calc-only (100%) but could not be re-verified live.** This does not block merging the rest of batch 8: `lithium_orotate` (like `nmn`) is excluded from the merge regardless, and the three compounds actually merging (colostrum, dextrose_carbs, electrolytes) passed live AI 9/9 (100%) before the balance ran out — judging "the batch's labels" as the labels for what is actually going live, consistent with how every other held compound's labels have been treated this run. **No further `make golden-live`/`make normalise`/`make content` (or anything else that calls the API) is possible until Patrick adds credit** — flagged plainly in the final report; everything after this point in the run is free (independent audits, `make check`, `make price`/`make export` on cached data, `make golden` calc-only/replay). | Real account constraint, discovered mid-run | Keep retrying (rejected: the error is a hard balance stop, not transient); hold all of batch 8 including the three unaffected compounds (rejected: no instruction requires holding compounds whose own labels passed cleanly just because a held compound's labels, in the same file, could not be re-checked) | batch 8, this run's remaining paid-step plans |
| 2026-09-19 | **Standing delegation for this run (Patrick away, asked to run to completion): a batch, or any single supplement in it, may be merged and pushed without a per-batch "approve" message from him**, provided ALL hold: rules 100%; live AI ≥ 90% on the batch's labels; an independent auditor subagent that did not draft the batch has no open blocking findings; the full golden set passes at least as well as before; `make check` green. Order this run: batch 5 (already drafted, audit/fix/merge/push, no API calls expected) → batch 6 (`k`-ids: vitamin B5, BCAA, EAA, ornithine, betaine HCl, agmatine, sodium bicarbonate, glycerol, CLA, beetroot/nitrate, flaxseed oil ALA, evening primrose GLA, black seed oil, silica, zinc carnosine) → batch 7 (`m`-ids: chaga, American ginseng, glucomannan, inulin, PHGG, sea moss, black pepper/piperine, beta-glucan, uridine, DMAE) → batch 8 (`n`-ids: NMN, lithium orotate, colostrum, carbohydrate powders, electrolytes — colostrum/carbs follow the collagen `simple_mass` precedent, electrolytes follow the multivitamin `per_serving` precedent). Probiotics: proposal only this run, not built. Digestive enzymes and everything in Appendix F.11 stay out. Budget authorised for this run: **£2.00** (running total before this run: ~£5.25 of the £10 cap) — estimated and logged before every paid step, stopped if the next step would exceed it. Hard stops (hold and ask instead): no existing compound id changes, no SQLite schema change, no `config/llm.yml` model or prompt-version change, never print keys. | Patrick will not be present for this run; keeps Appendix F moving without waiting for per-batch sign-off, while holding every batch to the same bar (golden thresholds, independent audit, `make check`) that batches 1-5 already met | Wait for a per-batch "approve" message (rejected: contradicts "work through to the end without waiting for me"); skip the independent audit (rejected: explicitly required this run); relax the golden threshold (rejected: no instruction to do so) | registry, all batches this run |
| 2026-09-19 | **Capsule refinement pass (Patrick: bring the site up to mock-up B2; "looks plain on desktop", "the home page"; plan approved).** Built on branch `ui-capsule-refinement` in six stages: (1) type scale with desktop display sizes; rank number moved to the capsule's darker lower half (white text passes AA; the grey off-state darkened to `#6b6f80`); FAQ questions styled as questions; quick-compare fixes (undefined token, no-JS 404 → hidden without JS). (2) **Every product row is a B2 card at every width** (`OfferRow.astro`): rank capsule, pick label above the name, a meta line (brand · retailer · pack · per serving — Pack and Per serving are no longer columns), three tiles with the per-dose price tinted and biggest, the buy capsule, the working; cards line up in columns on wide screens (container queries: tiles and capsule under each other on laptops, one line from 70rem); the cheapest card gets a teal ring and its working open, which follows the cheapest row until the visitor opens or closes one; label quotes fold under "What the retailer's listing says"; the working moved into the row, so there is no separate detail row. Still a table underneath, with explicit roles, a caption and hidden headings. Forms found only in combination products (32 of 56 pages) get a message and a "Show the n combination products" button. (3) Price pages: a bigger heading, "What it *really* costs per 100 mg magnesium, across 4 UK retailers. A unit for comparing, not advice.", and a tile with the cheapest price and whose it is; the controls panel became a slim sticky bar (your dose, then "Sort and filter (n)" in one panel; URL state unchanged); the explanation moved below the prices; search-result titles kept. (4) Home: a real example card (the cheapest magnesium bisglycinate from the export, kept out of the search index), a one-line quick compare, a facts line, cards grouped by ingredient class with whose price it is and product/retailer counts; supplements found only in combination products are named under their group instead of vanishing from home. (5) Header: current section underlined, a header search box on wide screens (opens the home page's results), "Updated {date}"; phones: one-line scrolling menu. Footer in columns. (6) Product pages use the same cards; browse pages the home cards; the phone table rules now apply only to plain data tables (fixes the basket at 640–832 px); methodology copy corrected. No data, ranking or URL changed. | Brief §12.4 said the first build was "a faithful first application, not the finished design" | Keep a plain table on wide screens (rejected: the plainness Patrick objected to) | `site/src/` |
| 2026-09-19 | **Design rules settled by the refinement pass.** (a) Brief §12.4 rule 3 amended (v1.10): card rows at every width, aligned so figures scan side by side, instead of a plain table on wide screens. (b) The capsule stays in exactly three places: the home quick-compare button is now a plain teal pill ("Compare prices"), not a two-tone capsule. (c) Dark-mode teal `#1a8296` → `#177a8d` so white text on it passes AA (4.49 → 5.0:1); accent-coloured text in dark mode uses the link colour; the capsule's hover underlines instead of brightening. (d) Phones show a shorter commission line, `disclosure_short` in `config/site.yml` ("We may earn commission from our links. It never affects the order."), with the same "Full disclosure" link; desktop keeps the full sentence. (e) Form headings read properly via a small site-side rule plus a map keyed by class id (`formHeading` in `site/src/lib/format.ts`): "Thiamine (vitamin B1)" not "Thiamine (vitamin B1) Thiamine", "Other forms of zinc", "Bovine collagen"; page titles and share cards follow. `compounds.yml` untouched. (f) Focus rings follow each element's shape so capsules stay round. **Flagged for Patrick, not changed:** iron's bisglycinate class label is "Bisglycinate (gentle iron)" in `compounds.yml` — "gentle" reads as a tolerability claim (hard rule 4). | Needed to apply B2 consistently and meet rule 6 (AA) | — | brief §12.4, `site.yml`, `format.ts`, `tokens.css` |
| 2026-09-19 | **Probiotics proposal finalised (Patrick: "use recommended simplicity") — still a proposal, not built.** The three open questions from this run's report are resolved to the simplest option in each case, so a future session can draft the compound without re-asking: (1) **CFU convention** — take whichever figure the label states, at face value, exactly like every other `simple_mass`-style compound already does (no special manufacture-vs-expiry detection or adjustment code); if a label happens to state both, the model's own evidence-quote mechanism naturally surfaces which one was used, with no new logic required. (2) **Multi-strain products** — use the single combined CFU total as stated; per-strain amounts (if given) are stored as informational components only, the same treatment thymoquinone/IgG get elsewhere, never summed or used to override the total. (3) **Classes** — one single class to start, no split by strain, genus or delivery technology; revisit only if real feed data later shows meaningfully different products that don't belong in one table together. Still needs, before any of this can be drafted: a new `cfu_count` normalisation type (brief §6.5 lists it as "later" — a real code addition, a resolver function plus a `CFU` unit, not a compounds.yml-only change) and a `probiotics` compound entry using it; both remain **not built**, per the standing instruction. | Patrick's instruction, resolving this run's open questions | Leave the three questions open for a future session to re-ask (rejected: Patrick asked for the simple default now) | future `cfu_count` work, `docs/HANDOVER.md` §3 item 4 |
